export default function ImageSingle({ ctx }: { ctx: any }) {
  console.log(ctx);

  return (
    <>
      {/* <div>{ctx.image}</div>
      <div>{ctx.image.alt}</div> */}
      do nothing
    </>
  );
}
